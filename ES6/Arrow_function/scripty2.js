// cara kedua

// menghilangkan kata function dan menggantikan dengan => untuk fanction

const pondokProgrammer = (p) => {
  return p; // menggunakan return dan parameter nya
};

// aksinya (pemanggilan)
document.write(pondokProgrammer("pondok programmer"));
